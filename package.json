{
  "name": "openpeer-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/config/*",
    "packages/shared/*"
  ],
  "engines": {
    "node": ">=14.0.0"
  },
  "packageManager": "yarn@1.22.17",
  "devDependencies": {
    "turbo": "^1.6.3",
    "turborepo": "^0.0.1"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  }
}
